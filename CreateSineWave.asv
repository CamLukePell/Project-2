function [data] = CreateSineWave(Fs,freq)
    Fs = 44100;
    
    points = (0:1/Fs:5);
    data = (0:1/Fs:5)';
  
    w0 = 5000*2*pi;
    for n = 1:length(data)
       data(n) = sin(w0*points(n));
    end
    plot(points,data);
end

